import 'package:flutter/material.dart';

class BessShadowStyle {
  static const defaultBoxShadow = BoxShadow(
    color: Colors.black,
    spreadRadius: 0,
    blurRadius: 0,
    offset: Offset(4, 4),
  );
}
