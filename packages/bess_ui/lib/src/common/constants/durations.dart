class BessDurations {
  // Animations
  static const Duration animShort = Duration(milliseconds: 300);
  // Extended durations
  static const Duration animVeryShort = Duration(milliseconds: 150);
  static const Duration animTooltip = Duration(milliseconds: 200);
  static const Duration pressReleaseDebounce = Duration(milliseconds: 30);
}
