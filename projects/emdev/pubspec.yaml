name: emdev
publish_to: none
description: CLI tools for managing the EmberLight monorepo.
version: 0.1.0
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  args: ^2.4.2
  ansicolor: ^2.0.1
  ember_cli_utils:
    path: ../../packages/ember_cli_utils

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0

executables:
  emdev: # This allows running `dart pub global activate --source path .` and then `emdev.exe`